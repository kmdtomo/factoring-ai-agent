// Dummy telemetry export to fix build issues
export const telemetry = {
  enabled: false
};

// Export a dummy mastra object to fix the reference error
export const mastra = {};

export default mastra;